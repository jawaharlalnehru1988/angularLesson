export interface NameObj{
    id: number
    name: string
    value: number
}

export interface AddressData {
    id: number
    street: string
    postcode: string
    name: string
    door: number | string
  }